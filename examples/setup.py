#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='flask-oauthprovider-examples',
      version='0.0.1',
      packages=['demoprovider'],
      install_requires=[
      ]
)

